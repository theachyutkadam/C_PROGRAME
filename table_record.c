#include "stdio.h"
int table(int record) {
  for (i = 0; i <= record; i++) {
    if (i==10) {
      continue;
    }
  }

  int main(int argc, char const *argv[]) {
    int num = 0;
    table(100);
    printf("%d\n", num);
    return 0;
  }

}
